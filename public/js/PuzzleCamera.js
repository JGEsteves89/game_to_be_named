export default class PuzzleCamera {
	constructor(viewSize, puzzle, tileWidth, tileHeight) {
		this.viewSize = viewSize;
		this.puzzle = puzzle;
		this.tileWidth = tileWidth;
		this.tileHeight = tileHeight;
	}
}
